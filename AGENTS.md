build
------

* build via `make all`

workspace deployment
--------------------

* use the workspace's `starfleet-bootstrap` script
* if target binary is locked, rm it first
* restart webserver and timer server

commits
-------

* always keep documentation in sync with the code
* needs signed-off by author
* self-consistent, minimal and complete (only one issue per commit)
